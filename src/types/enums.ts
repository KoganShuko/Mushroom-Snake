export enum GameKey {
  UP = 38,
  DOWN = 40,
  LEFT = 37,
  RIGHT = 39,
}

export enum Direction {
  UP,
  RIGHT,
  DOWN,
  LEFT,
  NONE,
}

export enum SnakePartTypes {
  HEAD,
  STRAIGHT,
  TURNING,
  TAIL,
}
